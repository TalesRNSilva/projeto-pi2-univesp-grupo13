# O que é

Essa pasta deve carregar os módulos definindo as rotas a serem implementadas no back-end. 

E.g. arquivo `obras.py` pode implementar o endpoint /obras e o /obra/<id>.

Posteriormente, esses endpoints são importados em main.py para o fluxo de execução principal do app