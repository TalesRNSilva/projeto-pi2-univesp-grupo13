# Copie este arquivo para ".env" e preencha com os dados reais do banco.
# NUNCA commitem o ".env" real (ele já está no .gitignore).
DATABASE_URL=mysql+pymysql://usuario:senha@host:3306/nome_do_banco
